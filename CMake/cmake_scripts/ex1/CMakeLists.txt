PROJECT(hello) 
CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
ADD_SUBDIRECTORY(src)
#set current directory path to DIR_SRC
AUX_SOURCE_DIRECTORY(. DIR_SRCS)  
ADD_EXECUTABLE(hello ${DIR_SRCS})
TARGET_LINK_LIBRARIES(hello User)
